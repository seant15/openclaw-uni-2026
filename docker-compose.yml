# Coolify default compose path (repo root).
# Production stack: coollabsio images, port 9090 for Traefik.
# Canonical copy also at deploy/coolify/docker-compose.yml — keep both in sync.

include:
  - deploy/coolify/docker-compose.yml
