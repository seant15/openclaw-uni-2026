*** NOTE ***
This is a PROPOSED patch (Option 2). Do NOT apply until Sean explicitly approves.

--- a/MEMORY.md
+++ b/MEMORY.md
@@
 ## Systems & config decisions (OpenClaw)
-- **Default Model:** `kimi/kimi-k2.5` (configured 2026-02-26)
-- **Fallback Chain:** `openai/gpt-5.2` → `anthropic/claude-sonnet-4-5` → `openai/gpt-4o-mini`
-- Multi-model strategy: default primary should not depend on a single provider's quota.
-- Use provider-diverse fallbacks (e.g., OpenAI ↔ Anthropic ↔ Kimi).
-- Prefer dedicated "lanes" via agent IDs (planner/executor/verifier, writer, etc.) instead of one agent doing everything.
+
+Historical restructuring / migration notes moved to:
+- `/data/workspace/security/reference/memory-archive/2026-Q1-openclaw-agent-restructure.md`
+
+Keep here only what is CURRENT and operationally relevant.
@@
-## Agent Configuration (2026-02-26 Fixes)
-[DELETE entire historical section through "Directory Structure Alignment" and "Complete Base Document Set" blocks]
+## Historical notes
+Moved to archive: `/data/workspace/security/reference/memory-archive/2026-Q1-openclaw-agent-restructure.md`
